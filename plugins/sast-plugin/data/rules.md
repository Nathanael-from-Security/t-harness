| Rule ID | Category | Source | Severity | Description | Detect Pattern |
|---|---|---|---|---|---|
| owasp-a01-broken-access-control | Access Control | OWASP Top 10 2021 | High | Server-side access control is missing, misordered, or too broad on web entry points, WebAPI resolvers, pluginfile callbacks, controllers, and CRUD model paths. | Web-accessible PHP files, GraphQL resolvers, mutations, pluginfile callbacks, or model methods that read/write records or files before `require_login()`, `require_capability()`, `has_capability()`, ownership/context checks, or `require_sesskey()` for state changes; `$DB->get_record()` or entity lookup by user-controlled `id` without record ownership/context validation |
| owasp-a02-cryptographic-failures | Cryptography | OWASP Top 10 2021 | High | Sensitive data, credentials, secrets, tokens, or transport security rely on weak crypto, weak randomness, plaintext storage, or disabled TLS validation. | Security-sensitive token/secret/password generation using `rand()`, `mt_rand()`, `uniqid()`, `array_rand()`, `md5()`, `sha1()`, DES/3DES/RC4/ECB, `mcrypt_*`, `CURLOPT_SSL_VERIFYPEER` set to false, hardcoded keys/passwords/tokens, plaintext secrets written with `$DB->insert_record()`, `$DB->update_record()`, or `set_config()`, or sensitive endpoints using `http://`, `ftp://`, or cleartext connection strings |
| owasp-a03-injection | Injection | OWASP Top 10 2021 | High | Untrusted input or stored attacker-controlled data reaches an interpreter, query, command, include, redirect, or HTML output context without parameterisation, escaping, or strict validation. | `required_param()`, `optional_param()`, superglobals, request bodies, CLI args, or DB/file values concatenated/interpolated into SQL, `LIKE`, shell commands, `eval()`, `assert()`, `call_user_func()`, dynamic class names, `include`/`require`, LDAP/XPath/header/mail functions, file paths, redirects, or `echo`/template output without the expected placeholder, escaping, allowlist, or Totara formatting helper |
| owasp-a04-insecure-design | Insecure Design | OWASP Top 10 2021 | High | A high-value flow lacks an explicit security control such as rate limiting, replay prevention, tenant/ownership boundaries, payload bounds, or resource limits. | Authentication, webhook publishing, queue processing, export/list queries, bulk actions, or privileged workflows with no throttling/backoff, nonce/timestamp/replay check, payload size limit, tenant/context ownership check, domain invariant check, bounded pagination, or resource cap; look for `PHP_INT_MAX`, unbounded `limit`, fanout loops, queue consumers, regex processing, or external requests controlled by users/config |
| owasp-a05-security-misconfiguration | Security Misconfiguration | OWASP Top 10 2021 | Medium | Production-reachable code exposes debug output, stack traces, unsafe defaults, insecure parser/runtime options, or unnecessary diagnostic features. | Non-test web paths containing `phpinfo()`, `var_dump()`, `print_r()`, `debug_backtrace()`, `ini_set('display_errors', 1)`, echoed exception messages/traces, default/test credentials outside test-only fixtures, permissive CORS/header configuration, XML parsing with external entities enabled, or diagnostic/admin code reachable without `require_login()` and capability checks |
| owasp-a06-vulnerable-and-outdated-components | Dependencies | OWASP Top 10 2021 | Medium | Repository-visible dependencies, vendored libraries, or platform version declarations are missing, unsupported, fetched insecurely, or visibly stale. | `composer.json`, `composer.lock`, package manifests, vendored libraries, plugin version files, or build scripts with missing/ambiguous versions, unpinned dependencies, deprecated/unmaintained packages, insecure `http://` package sources, checked-in vendor code with old version banners, or comments/config that disable dependency/security updates |
| owasp-a07-identification-and-authentication-failures | Authentication | OWASP Top 10 2021 | High | Authentication, session, token, or credential-handling code permits weak identity proofing, predictable credentials, insecure token comparison, or missing login enforcement. | Login/token/session/password-reset/HMAC code using weak token generation, hardcoded/default credentials, empty-password LDAP binds, plaintext or weak password hashing, non-constant-time secret comparison instead of `hash_equals()`, missing `require_login()` on authenticated pages/resolvers, missing `session_regenerate_id(true)` after auth state changes, or `setcookie()` for session/auth cookies with Secure or HttpOnly disabled |
| owasp-a08-software-and-data-integrity-failures | Integrity | OWASP Top 10 2021 | High | Untrusted code, classes, files, packages, serialized data, or update inputs can change executable behavior without integrity validation. | `unserialize()`, `yaml_parse()`, `eval()`, `assert()`, dynamic `include`/`require`, dynamic `new $class`, `call_user_func()`/reflection from user or stored config, factory classes instantiating user-controlled names without `class_exists()`/interface allowlists, package/update downloads without signature/hash verification, or CI/build scripts executing downloaded content |
| owasp-a09-security-logging-and-monitoring-failures | Logging and Monitoring | OWASP Top 10 2021 | Medium | Security-relevant actions are not audited, or logging leaks sensitive data or implementation detail instead of useful defensive context. | Create/update/delete/secret-rotation/authentication/authorization/webhook-delivery paths with no Totara event/audit/log emission, authorization denials not logged where security-relevant, `error_log()` or logger calls containing raw request bodies, payloads, tokens, secrets, PII, or HMAC material, or exception handlers that expose stack traces/query details to users without a separate safe internal log |
| owasp-a10-server-side-request-forgery | SSRF | OWASP Top 10 2021 | Medium | Server-side HTTP/file/socket requests can be directed to user-controlled destinations without strict scheme, host, port, redirect, and internal-network controls. | `curl_setopt(..., CURLOPT_URL, ...)`, `file_get_contents()`, `fopen()`, `fsockopen()`, webhook publishers, URL preview/import/fetch features, or stored endpoint fields using request/user/config-controlled URLs without a positive allowlist for scheme/host/port, DNS/IP validation for localhost/private/metadata ranges, redirect disabling, response filtering, and bounded timeout/size controls |
| php-injection-code | Injection | PHP | High | User-controlled data is passed to PHP code execution sinks. | `eval($userInput)`, `assert($userInput)`, `preg_replace('/.../e', $var, ...)`, `create_function($userInput)`, or `call_user_func($userControlledCallback, ...)` without strict allowlist validation |
| php-injection-command | Injection | PHP | High | User-controlled data reaches shell execution without safe argument handling. | `exec($var)`, `system($var)`, `shell_exec($var)`, `passthru($var)`, `popen($var, ...)`, or backtick execution with data from params, superglobals, CLI args, DB, files, or config and no `escapeshellarg()`/argument allowlist |
| php-injection-sql-concat | Injection | PHP | High | User input is concatenated into SQL instead of passed through placeholders. | SQL built with `"SELECT ... " . $var`, interpolated variables, or `sprintf()` and passed to `$DB->get_records_sql()`, `$DB->get_record_sql()`, `$DB->execute()`, PDO, or mysqli without bound parameters |
| php-injection-stored-code | Injection | PHP | High | Previously stored user data is later executed as PHP code. | DB/file/cache/config value passed to `eval()`, `assert()`, `create_function()`, `preg_replace('/.../e', ...)`, or dynamic callback invocation |
| php-injection-stored-command | Injection | PHP | High | Previously stored user data is later executed by the shell. | DB/file/cache/config value passed to `exec()`, `system()`, `shell_exec()`, `passthru()`, `popen()`, or backticks without strict allowlist and safe argument construction |
| php-xss-reflected-output | XSS | PHP | High | User-controlled request input is reflected into HTML without escaping. | `echo`, `print`, inline templates, or response builders outputting `$_GET`, `$_POST`, `$_REQUEST`, `required_param()`, `optional_param()`, or derived variables without `s()`, `htmlspecialchars()`, `format_string()`, or `format_text()` as appropriate |
| php-file-inclusion-user-input | File Inclusion | PHP | High | User-controlled data selects a PHP file to include or require. | `include`, `require`, `include_once`, or `require_once` using superglobals, request params, route params, DB/config values, or concatenated paths without fixed allowlist resolution |
| php-dangerous-eval-use | Code Execution | PHP | High | `eval()` is present in application code and should be treated as dangerous even when taint is not immediately proven. | Any `eval(...)` call outside tests or intentionally isolated developer tooling |
| php-dangerous-exec-use | Command Execution | PHP | High | Shell execution APIs are present and require review for taint, argument handling, and timeout controls. | Any use of `exec()`, `shell_exec()`, `system()`, `passthru()`, `popen()`, or `proc_open()` outside tightly controlled maintenance tooling |
| php-command-backticks | Command Execution | PHP | High | PHP backtick shell execution is used. | Backtick operator containing variable interpolation or any non-static command |
| php-code-mb-ereg-replace-eval | Code Execution | PHP | High | `mb_ereg_replace()` uses eval mode. | `mb_ereg_replace($pattern, $replacement, $subject, 'e')` or options containing `e` |
| php-code-assert-string | Code Execution | PHP | High | `assert()` receives a string or string-derived value that can be evaluated as PHP code. | `assert("$value")`, `assert($stringVar)`, or assert input derived from request, DB, file, cache, config, or CLI args |
| php-deserialization-untrusted | Deserialization | PHP | High | Untrusted serialized data can instantiate PHP objects. | `unserialize()` called on superglobals, request params, file contents, queue payloads, DB/cache/config values, or any non-static value without `allowed_classes => false` and trusted provenance |
| php-secret-plaintext-storage | Secrets | PHP | High | Secrets are stored in plaintext at rest. | OAuth, API, webhook, HMAC, client, or password secrets written using `$DB->insert_record()`, `$DB->update_record()`, `set_config()`, files, fixtures, or logs without hashing, encryption, or secret-store handling |
| php-resource-exhaustion-unbounded | Resource Exhaustion | PHP | High | User-influenced processing can consume excessive CPU, memory, database, network, or process resources. | User-controlled regex, loops, recursion, subprocesses, fanout, queue processing, external calls, file reads, JSON/XML parsing, or list queries without timeout, count, byte, depth, or pagination bounds |
| php-xxe-external-entities | XXE | PHP | High | XML parsing can resolve external entities or network resources. | `libxml_disable_entity_loader(false)`, `LIBXML_NOENT`, `LIBXML_DTDLOAD`, `simplexml_load_string()`, `DOMDocument::loadXML()`, or XMLReader usage without `LIBXML_NONET` and entity hardening |
| php-xss-stored-output | XSS | PHP | Medium | Stored user-controlled content is rendered without escaping or formatting. | DB/cache/file/entity fields such as names, descriptions, payloads, messages, event data, or HTML content output with `echo`, `print`, templates, or formatters without `s()`, `format_string()`, `format_text()`, or explicit trusted HTML policy |
| php-injection-sql-second-order | Injection | PHP | Medium | Stored data is retrieved and later concatenated into SQL. | `$DB->get_field()`, `$DB->get_record()`, entity property, config, or file value reused in `$DB->get_records_sql()`, `$DB->execute()`, PDO, or mysqli SQL string without placeholders |
| php-path-traversal-user-input | Path Traversal | PHP | Medium | User input is used in filesystem paths without canonical directory validation. | `file_get_contents()`, `fopen()`, `readfile()`, `unlink()`, `copy()`, `rename()`, `move_uploaded_file()`, or archive extraction using request params without `realpath()` containment or strict filename allowlist |
| php-path-traversal-stored | Path Traversal | PHP | Medium | Stored data is used in filesystem paths without containment validation. | DB/cache/config/file value used in `fopen()`, `file_get_contents()`, `readfile()`, `unlink()`, `copy()`, `rename()`, or include paths without `realpath()` containment |
| php-ssrf-user-url | SSRF | PHP | Medium | User-controlled URL is fetched by the server. | `curl_setopt($ch, CURLOPT_URL, $userInput)`, `file_get_contents($userInput)`, `fopen($userInput, 'r')`, HTTP clients, or stream wrappers using request/user/config URL without allowlisted scheme, host, port, and private-IP rejection |
| php-open-redirect-stored | Open Redirect | PHP | Medium | Stored data controls a redirect target. | `header("Location: " . $dbValue)`, redirect helpers, or `moodle_url` construction using DB/config/profile values without local-path or host allowlist validation |
| php-crypto-insecure-random | Cryptography | PHP | Medium | Predictable PRNG is used for security-sensitive material. | `rand()`, `mt_rand()`, `array_rand()`, `uniqid()`, `microtime()`, or hashes of these values used for tokens, passwords, HMAC secrets, CSRF tokens, session identifiers, reset links, or webhook secrets |
| php-transport-db-no-tls | Transport Security | PHP | Medium | Database or service connection strings do not enforce TLS. | Connection strings using `mysql://`, `pgsql://`, `ldap://`, `http://`, or PDO/service configs without SSL/TLS options such as CA verification |
| php-dos-sleep-user-controlled | Resource Exhaustion | PHP | Medium | User input controls sleep duration. | `sleep($userInput)`, `usleep($userInput)`, retry/backoff values, queue delay, or throttle duration derived from request, config editable by users, DB, or CLI args without small bounds |
| php-ldap-empty-password-bind | Authentication | PHP | Medium | LDAP bind allows empty or null password. | `ldap_bind($conn, $dn, "")`, `ldap_bind($conn, $dn, null)`, or password value not checked for empty before LDAP bind |
| php-curl-ssl-verifypeer-disabled | Transport Security | PHP | Medium | cURL certificate verification is disabled. | `curl_setopt($ch, CURLOPT_SSL_VERIFYPEER, false)` or `CURLOPT_SSL_VERIFYHOST` set to `0` or `false` outside test-only code |
| php-crypto-decrypt-without-hmac | Cryptography | PHP | Medium | Ciphertext is decrypted without integrity authentication. | `openssl_decrypt()` used without prior HMAC/signature/AEAD tag validation or without an authenticated cipher mode |
| php-crypto-mcrypt-use | Cryptography | PHP | Medium | Deprecated `mcrypt_*` cryptography is used. | Any `mcrypt_encrypt()`, `mcrypt_decrypt()`, `mcrypt_create_iv()`, or related `mcrypt_*` function |
| php-crypto-md5-sha1-loose-comparison | Cryptography | PHP | Medium | MD5 or SHA1 hash is compared loosely, allowing type juggling. | `md5($input) == $hash`, `sha1($input) == $hash`, or loose comparison involving user-controlled digest strings instead of `hash_equals()` |
| php-password-sha224-hash | Cryptography | PHP | Medium | SHA-224 is used as a password hash instead of a password hashing API. | `hash('sha224', $password)`, `sha224($password)`, or custom SHA-224 password verifier |
| php-token-base-convert-precision | Cryptography | PHP | Medium | `base_convert()` is used on large hashes or tokens and can lose precision. | `base_convert(hash('sha256', ...), 16, 36)`, `base_convert($largeDigest, ...)`, or token derivation from large numeric strings |
| php-secret-hardcoded-credential | Secrets | PHP | Low | Passwords, API keys, tokens, or secrets are committed directly in source. | `$password = "secret"`, `define('API_KEY', '...')`, `$token = '...'`, `$secret = '...'`, fixture credentials outside test-only scope, or sample credentials used by runtime code |
| php-transport-insecure-protocol | Transport Security | PHP | Low | Sensitive service traffic uses a cleartext protocol. | `ftp://`, `http://`, `smtp://`, `ldap://`, or other cleartext URLs used for credentials, callbacks, secrets, webhooks, APIs, or administrative communication |
| php-transport-ftp-plaintext | Transport Security | PHP | Low | FTP is used without TLS. | `ftp_connect()` or `ftp://` URLs where `ftp_ssl_connect()` or a secure transport should be used |
| php-transport-insecure-port | Transport Security | PHP | Low | Insecure service ports are hardcoded for sensitive communication. | Hardcoded ports such as 21, 23, 25, 80, 110, or 143 in service clients, connection strings, webhook endpoints, or admin integrations without TLS context |
| php-open-redirect-user-input | Open Redirect | PHP | Low | User input controls a redirect target. | `header("Location: " . $_GET['url'])`, redirect helpers, `redirect($url)`, or response Location headers using request params without same-origin or allowlist validation |
| php-info-stack-trace-exposure | Information Disclosure | PHP | Low | Exception details or stack traces are exposed to users. | `echo $e->getMessage()`, `print_r($e->getTrace())`, `debug_print_backtrace()`, `var_dump($e)`, or uncaught exception output in production-reachable code |
| php-tempfile-shared-directory | File Safety | PHP | Low | Temporary files are created unsafely in shared directories. | `tempnam('/tmp', ...)`, `tmpfile()`, predictable temp paths, or `sys_get_temp_dir()` usage for sensitive content without permissions, atomic open, or cleanup |
| php-response-header-injection | Injection | Semgrep PHP | High | User input is inserted into HTTP headers and may enable response splitting. | `header("X-Name: " . $userInput)`, `header($userControlledHeader)`, or response header helpers using request/user data without CRLF rejection |
| php-ldap-filter-injection | Injection | Semgrep PHP | High | User input is concatenated into LDAP filters. | `ldap_search($conn, $base, "uid=" . $userInput)`, LDAP filter strings with request/user data, or unescaped LDAP OR filter construction |
| php-xpath-query-injection | Injection | Semgrep PHP | High | User input is concatenated into XPath queries. | `$xpath->query("/users/user[@name='" . $userInput . "']")` or XPath strings built from request/user data without XPath escaping |
| php-mail-header-injection | Injection | Semgrep PHP | High | User input is inserted into mail headers. | `mail($to, $subject, $body, "CC: " . $userInput)`, `From`, `Reply-To`, `Bcc`, or arbitrary mail headers built from request/user data without CRLF rejection |
| php-xss-reflected-superglobal | XSS | Semgrep PHP | High | Superglobal input is directly reflected into HTML. | Direct `echo`, `print`, or template interpolation of `$_GET`, `$_POST`, `$_REQUEST`, `$_COOKIE`, or request arrays without escaping |
| php-file-upload-tainted-filename | File Upload | Semgrep PHP | High | User-controlled upload filename is trusted for storage path. | `move_uploaded_file($tmp, $_POST['name'])`, `$_FILES['file']['name']` used in destination path, or upload filename not normalized and allowlisted |
| php-crypto-des-3des-use | Cryptography | Semgrep PHP | High | DES or 3DES encryption is used. | `openssl_encrypt()` or `mcrypt_encrypt()` with `DES`, `DES-EDE`, `3DES`, or `MCRYPT_DES` |
| php-crypto-rc4-use | Cryptography | Semgrep PHP | High | RC4 encryption is used. | `openssl_encrypt(..., 'RC4')`, `ARCFOUR`, or equivalent RC4 cipher selection |
| php-password-md5-hash | Cryptography | Semgrep PHP | High | MD5 is used for password hashing. | `md5($password)`, `md5($pass . $salt)`, or password verification comparing MD5 digests instead of `password_hash()`/`password_verify()` |
| php-password-sha1-hash | Cryptography | Semgrep PHP | High | SHA1 is used for password hashing. | `sha1($password)`, `sha1($pass . $salt)`, or password verification comparing SHA1 digests instead of `password_hash()`/`password_verify()` |
| php-session-weak-random-id | Authentication | Semgrep PHP | Medium | Session identifiers are generated from weak randomness. | `session_id(md5(rand()))`, `session_id(uniqid())`, `md5(mt_rand())`, or custom session token using predictable PRNG |
| php-crypto-ecb-mode | Cryptography | Semgrep PHP | Medium | ECB cipher mode is used. | `openssl_encrypt(..., 'AES-128-ECB')`, `AES-256-ECB`, or cipher names ending in `-ECB` |
| php-session-fixation-missing-regeneration | Authentication | Semgrep PHP | High | Session ID is not regenerated after authentication state changes. | Login, impersonation, privilege elevation, or account-linking code that authenticates a user without `session_regenerate_id(true)` or framework-equivalent rotation |
| php-secret-comparison-timing | Cryptography | Semgrep PHP | Medium | Secrets or tokens are compared with variable-time equality. | HMACs, API keys, CSRF tokens, reset tokens, webhook signatures, or session secrets compared with `==`, `===`, `strcmp()`, or string operators instead of `hash_equals()` |
| php-password-weak-length-only | Authentication | Semgrep PHP | Medium | Password strength policy only checks length or a weak condition. | `strlen($password) < 8`, one-rule password checks, or password accept paths with no complexity, blocklist, or policy helper where a stronger local framework helper exists |
| php-cookie-httponly-disabled | Session Security | Semgrep PHP | Medium | Auth/session cookies are created without HttpOnly. | `setcookie()` or cookie option arrays where auth/session cookie has `httponly` false or omitted in code that does not rely on secure framework defaults |
| php-cookie-secure-disabled | Session Security | Semgrep PHP | Medium | Auth/session cookies are created without Secure. | `setcookie()` or cookie option arrays where auth/session cookie has `secure` false or omitted for HTTPS-only applications |
| php-file-upload-unrestricted | File Upload | Semgrep PHP | High | File upload accepts arbitrary content without MIME or extension validation. | `move_uploaded_file()` or upload save path without allowlisted extension, MIME/content sniffing, size limit, and storage outside executable webroot |
| php-file-write-user-path | File Write | Semgrep PHP | High | User-controlled path or filename controls file writes. | `file_put_contents($userInput, $content)`, `fwrite()` to user-selected path, `copy()`, `rename()`, or generated files using request/user path components without containment validation |
| php-include-user-input | File Inclusion | Semgrep PHP | High | Dynamic include or require uses user input. | `include $userInput`, `require_once $_GET['module'] . '.php'`, or dynamic include path from route/user/config without fixed allowlist |
| php-tempfile-symlink-risk | File Safety | Semgrep PHP | Medium | Temporary file handling may be vulnerable to symlink or race attacks. | `tempnam()`, `sys_get_temp_dir()`, predictable names, or shared temp directories used before non-atomic open/write/chmod operations |
| php-debug-phpinfo-exposed | Information Disclosure | Semgrep PHP | Medium | `phpinfo()` is reachable outside test or local-only tooling. | `phpinfo()` in production, admin, public, install, index, route, or debug files without strict local/developer gate |
| php-debug-var-dump-production | Information Disclosure | Semgrep PHP | Medium | Debug dump output is reachable in production code paths. | `var_dump()`, `print_r()`, `dump()`, `dd()`, or raw object dumps not gated behind developer-only debug checks |
| php-debug-display-errors-enabled | Information Disclosure | Semgrep PHP | Medium | Runtime error display is enabled from application code. | `ini_set('display_errors', 1)`, `error_reporting(E_ALL)` with display enabled, or config enabling visible errors in production-reachable paths |
| php-debug-backtrace-exposed | Information Disclosure | Semgrep PHP | Low | Debug backtrace output is sent to the response. | `echo debug_backtrace()`, `print_r(debug_backtrace())`, `debug_print_backtrace()`, or exception trace printed to users |
| php-log-sensitive-data | Logging | Semgrep PHP | Low | Sensitive values are written to logs. | `error_log($password)`, `error_log($token)`, logger calls containing raw payloads, request bodies, credentials, HMAC secrets, API keys, session IDs, PII, or payment data |
| php-xxe-simplexml | XXE | Semgrep PHP | High | `simplexml_load_string()` parses untrusted XML without entity hardening. | `simplexml_load_string($input)` on request/file data without `LIBXML_NONET`, entity disabling, and DTD restrictions |
| php-xxe-domdocument | XXE | Semgrep PHP | High | `DOMDocument::loadXML()` parses untrusted XML without entity hardening. | `$dom->loadXML($input)` on request/file data without `LIBXML_NONET` and DTD/entity restrictions |
| php-yaml-parse-untrusted | Deserialization | Semgrep PHP | Medium | Untrusted YAML is parsed in a way that may instantiate objects or unsafe types. | `yaml_parse($userInput)`, YAML parsing of request/file data, or parser options that permit object tags without trusted schema |
| php-json-decode-missing-error-check | Input Validation | Semgrep PHP | Low | JSON parse failures are not checked before data use. | `json_decode($input)` followed by array/object access without checking `json_last_error()`, `JSON_THROW_ON_ERROR`, or `null` ambiguity |
| php-ssrf-curl-follow-redirect | SSRF | Semgrep PHP | Medium | cURL follows redirects while fetching user-controlled URLs. | `CURLOPT_FOLLOWLOCATION` enabled with user/config controlled `CURLOPT_URL`, no redirect host validation, or no final IP/private-range check |
| php-ssrf-fsockopen-user-host | SSRF | Semgrep PHP | Medium | User input controls `fsockopen()` destination. | `fsockopen($userInput, $port)`, user-controlled host/port sockets, or raw TCP clients without allowlist and private-IP rejection |
| php-ssrf-dns-rebinding-check | SSRF | Semgrep PHP | Low | DNS lookup validation can be bypassed by DNS rebinding or TOCTOU. | `gethostbyname($host)` or DNS pre-check used to allow outbound requests without validating the resolved IP used by the actual connection |
| php-code-preg-replace-eval | Code Execution | Semgrep PHP | High | `preg_replace()` uses the deprecated eval modifier. | `preg_replace('/pattern/e', $replacement, $str)` or regex patterns with `/e` modifier |
| php-variable-extract-user-input | Variable Injection | Semgrep PHP | High | `extract()` imports user input into the local symbol table. | `extract($_GET)`, `extract($_POST)`, `extract($_REQUEST)`, or `extract($requestData)` where keys are attacker-controlled |
| php-variable-register-globals-emulation | Variable Injection | Semgrep PHP | High | User-controlled names create variables dynamically. | `$$varName = $value`, loops assigning `$$key`, or register-globals emulation from request arrays |
| php-deserialization-object-injection | Deserialization | Semgrep PHP | High | `unserialize()` can instantiate attacker-controlled objects. | Any `unserialize()` on non-static input, queue payloads, DB values, files, cookies, or request data without safe `allowed_classes` and trusted provenance |
| php-type-juggling-loose-comparison | Type Juggling | Semgrep PHP | Medium | Loose comparisons can bypass security checks. | `==`, `!=`, `in_array(..., false)`, or `switch` comparisons involving user input, hashes, tokens, booleans, or magic values such as `"0e..."` |
| php-path-htaccess-bypass | Path Traversal | Semgrep PHP | Medium | User-controlled path may bypass webroot or `.htaccess` assumptions. | `readfile($userPath)`, direct file serving, or download handlers using path input that can escape intended directory or reach protected files |
| php-object-dynamic-class-instantiation | Code Execution | PHP | High | Factory code instantiates a class name that can be influenced by users, config, DB, queue payloads, or request data without strict allowlisting. | `new $class`, `new $handlerClass`, reflection instantiation, dependency containers, or factories using non-static class names without `class_exists()`, `is_subclass_of()`/interface checks, constructor side-effect review, and an explicit allowlist |
| php-resource-unbounded-pagination | Resource Exhaustion | PHP | Medium | List/query APIs remove practical pagination limits and can fetch/process excessive data in one request. | `PHP_INT_MAX`, `LIMIT` from user/config without maximum, disabled pagination caps, `get_records()`/repository calls with unbounded limits, or GraphQL/WebAPI list resolvers that let one request enumerate all rows |
| totara-input-raw-superglobal | Input Validation | Totara/Moodle | High | Raw PHP superglobals are read instead of Totara parameter APIs. | Direct `$_GET`, `$_POST`, `$_REQUEST`, `$_COOKIE`, or raw request body use outside bootstrap/config/test code instead of `required_param()`, `optional_param()`, or explicit request validation |
| totara-input-wrong-param-type | Input Validation | Totara/Moodle | Medium | Totara parameter validation uses an overly permissive type. | `required_param('id', PARAM_TEXT)`, `optional_param('id', ..., PARAM_RAW)`, or numeric/boolean/alpha fields using `PARAM_TEXT`, `PARAM_RAW`, or broad validation where `PARAM_INT`, `PARAM_BOOL`, `PARAM_ALPHANUM`, or a stricter type is expected |
| totara-auth-missing-login | Authentication | Totara/Moodle | High | Web-accessible PHP entry point is missing `require_login()`. | Entry point PHP files outside tests/classes that include config and render/read/write application data without `require_login()` before business logic |
| totara-authz-missing-capability | Authorization | Totara/Moodle | High | Privileged or state-changing action lacks a capability check. | POST handlers, form actions, admin pages, controllers, ajax/WebAPI mutations, model write methods, pluginfile callbacks, or sensitive reads without `require_capability()` or verified `has_capability()` in the relevant context |
| totara-csrf-missing-sesskey | CSRF | Totara/Moodle | High | State-changing POST handler lacks `require_sesskey()`. | `$_SERVER['REQUEST_METHOD'] === 'POST'`, submitted forms, ajax actions, delete/update/create/rotate/requeue operations, or mutation-like entry points that write state before `require_sesskey()` |
| totara-sql-concat | Injection | Totara/Moodle | High | SQL passed to Totara DML APIs concatenates variables instead of using placeholders. | `$DB->get_records_sql()`, `$DB->get_record_sql()`, `$DB->execute()`, repository query builders, or report SQL containing `" . $var`, interpolated variables, or `sprintf()` without parameter arrays |
| totara-output-raw-content | XSS | Totara/Moodle | High | Raw stored or request content is output without Totara formatting helpers. | `echo $record->content`, formatter returns, controller responses, GraphQL fields, or templates rendering arbitrary DB/request content without `s()`, `format_string()`, `format_text()`, or a trusted HTML policy |
| totara-file-dataroot-direct-read | File Access | Totara/Moodle | Medium | Code reads from `$CFG->dataroot` directly instead of using Totara's File API and capability checks. | `file_get_contents($CFG->dataroot . '/' . $path)`, `fopen($CFG->dataroot...)`, `readfile()`, or direct filesystem access to user/course/plugin files |
| totara-pluginfile-missing-capability | Authorization | Totara/Moodle | High | `pluginfile` callback serves files without checking capability and file context. | `_pluginfile()` implementation missing `has_capability()`/context validation, filearea/itemid ownership checks, or access checks before returning stored files |
| totara-webapi-missing-capability-middleware | Authorization | Totara/Moodle | High | WebAPI resolver or mutation lacks capability middleware for protected reads or writes. | Resolver/mutation classes missing capability middleware, interactor checks, or context capability validation before reading sensitive records or performing create/update/delete/rotate actions |
| totara-authz-idor | Access Control | Totara/Moodle | High | Record is fetched by a user-controlled ID without ownership or context verification. | `$DB->get_record('table', ['id' => $id])`, entity/repository `find($id)`, model loaders, GraphQL `id` arguments, or controller IDs where the loaded record is returned/modified without checking context, tenant, owner, or capability over that record |
| totara-audit-crud-missing-event | Logging and Monitoring | Totara/Moodle | Medium | Create, update, delete, rotate, requeue, or privileged read action is performed without a Totara event or audit emission. | Model `create()`, `update()`, `delete()`, mutation execute methods, secret rotation, DLQ requeue/purge, webhook delivery/admin actions, or capability-protected state changes with no `\core\event\...::create()->trigger()` or local event/audit helper |
| totara-config-global-scope-misuse | Configuration | Totara/Moodle | Low | Global config such as `$CFG` is used inside a function, method, or closure without declaring it global or passing it explicitly. | `$CFG`, `$DB`, `$USER`, `$PAGE`, or `$OUTPUT` referenced inside functions, class methods, closures, or callbacks without `global $CFG;`, `global $DB;`, etc., constructor injection, or explicit parameter passing |
| totara-webhook-ssrf-user-endpoint-lifecycle | SSRF | Totara/Moodle | High | User-controlled webhook endpoints are accepted in create/update flows and later used by the publisher without a consistent outbound allowlist. | Webhook create/update mutation, model, or config accepts endpoint URLs from users and `totara_webhook_publisher` or equivalent later passes stored URL to cURL/HTTP client without enforcing HTTPS, host/port allowlist, private-IP/metadata blocking, redirect disabling, timeout, response-size cap, and revalidation at publish time |
